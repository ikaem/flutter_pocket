Future<void> main() async {
// apparently not good becuase it does not keep implementaiton details separate from the business logic for some reason
  // final WeatherRepository weatherRepository = WeatherAppRepository();
  // final double temp = weatherRepository.getTemperature("Pula");

  // // we can do this now because we added an unnamed factory cosntrutor, and it looks like we are intantiating abstract class - nto sure how i feel about this
  // final WeatherRepository weatherRepository = WeatherRepository();
  // final double temp = weatherRepository.getTemperature("PUla");
}
