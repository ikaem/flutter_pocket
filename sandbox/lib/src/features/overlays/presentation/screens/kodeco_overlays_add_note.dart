import 'package:flutter/material.dart';

class KodecoOverlaysAdd extends StatelessWidget {
  const KodecoOverlaysAdd({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.all(48),
      color: Colors.yellow,
      child: const Center(
        child: Text("Karlo"),
      ),
    );
  }
}
